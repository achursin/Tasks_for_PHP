
 * функции должны только создавать строку. выводить ее надо после, при помощи echo.
    выглядеть должно примерно так. Внутри функции bank() никаких echo
    $result = bank(....);
    echo $result;

    там где задачи на correct_* тоже можно так сделать. в функции будет сам regex и preg_match.
    эта функция будет принимать на вход слово, возвращать значение true/false, и в зависимости от этого потом уже печатам что либо (но это уже не в функции)

    $isCorrect = isCorrect('xxxxxxxxxxxxxxx');

    if ($isCorrect) {
        echo'yes';
    } else {
        echo 'No'
    }

 файлы, относящиеся к одной задаче, если их несколько, убрать в отдельные папки и корректо эти папки назвать.
 все  массивы заменить на новый синтаксис array() на []. погугли что такое новый синтаксис массивов

адача про вопросы
 при создании экземпляра класса question массив ответов тоже передавать в конструктор, и тоже хранить переменную в экземпляре класса
 убрать функцию nl2br. требуется как раз наоборот - использовать только PHP_EOL или "\n"
 добавить в класс Question метод, который возвращает текстовое представление вопроса, которое можно напечатать
 $question = new Question( ... )
 $string = $question->print();

 после этого использовать этот метод в функции printQuestions().

 в этой задаче разложить код на несколько файлов - класс question отдельно, функции createQuestions printQuestions checkAnswers в другой файл
 а рабочий код, который с ними работает - в третий.

 вот это рабочий код

 $questions = createQuestions();
 printQuestions($questions);
 $answers = array('a', 'b', 'd');
 $answersWord = array('Барселона', 'пять', 'в финале ЧЕ');
 checkAnswers($questions, $answers, $answersWord);

 при выводе результата непонятно, какие даны ответы на вопросы (когда правильно).
 Название класса с большой буквы, а название файла - с маленькой. Сделай тоже с большой. это важно.


// другие

 задача про correct_email почему то проверяет цифры
 непонятный вывод в задаче про рост. Имя зачем то на каждой второй строке

     Антон
    выше анона <br>Имя: Семен, рост: 165 см.
    Семен
    ниже анона <br>Имя: Лена, рост: 189 см.
    Лена
     ...
