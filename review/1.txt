1.1. убрать из вывода все теги html (например <br>) использовать перевод строки в PHP \n или PHP_EOL
Считай что я тестирую из консоли, и вывод сделай под консоль.
1.2. назови нормально файлы, чтобы понимать какая задача где лежит Task1 это не очень
1.3 * функции должны только создавать строку. выводить ее надо после, при помощи echo.
например у тебя функция bank() считает и сама печатает. напиши код так, чтобы она только составляла строку, а выводил бы ты ее после
при помощи echo
$result = bank( ... );
echo $result; вот так

1.4. $homoCredit = bank(1.04, 500,0,'$homoCredit'); в этом месте ты присваиваешь переменной результат работы функции, но функция у тебя ничего не возвращает
(нету return)

//задача про вопросы

2.1. комменты повторяются в каждой строке. у тебя есть уже комменты в классе, только там их и оставь
2.2. в функции printQuestions ты используешь переменную number чтобы выводить номер вопроса. Вместо нее используй
вот такой цикл foreach($questions as $key => $question). погугли как это работает

2.3. сам класс Question перенеси в отдельный файл. в том файле где у тебя обстнется работа с ним - вызови его через require
2.4. для класс Question сделай конструктор - погугли что это, который заполняет все его поля
2.5. Файл в котором будет лежать класс называй точно также как и класс, один в один.

2.6. убери <style>, я будут смотреть вывод только через консоль